/** Minimal-build stub for `$gallery/omni` — the omni 3D object UI is excluded from the core build. */
export class OmniUI {
	constructor(..._args: unknown[]) {}

	async setup(): Promise<void> {}

	goto(_i: number): void {}

	animateTo(_idx: number): void {}

	destroy(): void {}
}
