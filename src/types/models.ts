/**
 * # Micrio JSON data model
 * 
 * Re-exports all model types under the `Models` namespace.
 * 
 * This data is created in the [Micrio editor](https://dash.micr.io/), and published as static JSON file per image.
 */

export * as Models from './models/index';
